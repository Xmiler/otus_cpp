dist: focal
language: cpp
before_script:
- sudo apt-get install libboost-test-dev -y
script:
  - cmake .
  - cmake --build .
  - cmake --build . --target test
  - cmake --build . --target package
deploy:
  provider: script
  skip_cleanup: true
  script:
    - curl -T sparse_matrix-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb -uxmiler:$BINTRAY_API_KEY "https://api.bintray.com/content/xmiler/otus_cpp/hw4_sparse_matrix/$TRAVIS_BUILD_NUMBER/sparse_matrix-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb;deb_distribution=trusty;deb_component=main;deb_architecture=amd64;publish=1"
  on: hw5_sparse_matrix
